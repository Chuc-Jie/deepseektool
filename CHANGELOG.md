# DeepSeek 功能增强工具箱 — 更新日志

## v4.9.0 (2026-09-10)

### 新增
- **原生「置顶」分组支持折叠**：官网分组标题本无折叠能力（实测六个分组头均无按钮/箭头、`cursor:auto`）。现点击「置顶」标题即可收起/展开其会话列表，折叠态持久化（新键 `deepseek_pin_group_collapsed`）；折叠 = 隐藏该分组容器内除标题外的全部兄弟节点（置顶组的行在 wrapper 内、日期组的行是直接子节点，机制通用）。箭头经 `mask` 用伪元素绘制、点击用 document 级事件委托（`closest('.ds-pin-head')`），不往 React 管理的原生标题里塞节点、也不依赖官网 hash 类名；折叠态每轮 schedule 重放，React 重建后自动复原。原生「选择对话」多选态下不响应折叠。

### 修复
- **归档进文件夹的「置顶」会话会从置顶区消失**：`syncArchiveVisibility` 原先对所有归档会话行一律 `display:none`，不区分所处分组——置顶会话只要被收进文件夹，就会连带整组从「置顶」区消失（实测：脚本开启时置顶会话容器高度归 0，5 个置顶会话全部不可见）。现「置顶」分组内的行豁免隐藏，并在需要时还原显示。
- **文件夹面板插进了「置顶」标题与其会话之间**：原生结构为 `[置顶标题 → 置顶会话容器 → 分隔线]`，原挂载锚点取 sticky 标题之后，导致置顶会话被挤到文件夹面板下方、分组被切断。现锚点改为「置顶」**分组容器**之后（无置顶分组时兜底插入列表最顶），并保留旧位置的自修复迁移。
- **「置顶」与「文件夹」之间的 26px 死空白**：`#dsFolderPanel` 的 `padding-top:22px` 原为让开悬浮「多选」按钮，但实测该按钮位于「置顶」行带内（y139–151）、在面板（y158 起）之上，预留已失效。现默认 `2px` 对齐原生「分组头→首行」的零间距节奏，仅兜底挂载（无置顶分组）时才临时补 22px。

### 优化
- **原生多选（选择对话）模式下整区隐藏文件夹面板**：由「降透明度 + 禁用交互」改为 `display:none`，把空间让给原生批量选择，退出模式后自动恢复（检测 `.ds-checkbox` 是否为多选态专属标记）。
- **折叠箭头与标题对齐统一**：「文件夹」标题的折叠箭头由文字左侧移到右侧；「置顶」箭头由三角改为与「文件夹」完全同款的 chevron（同 path、同色 `--ds-sub`、收起同样旋转 -90°），hover 统一复用 `--ds-hover` 底色；文件夹标题左缘对齐到与原生「置顶」标签及文件夹行相同的竖线位置。

## v4.8.1 (2026-09-09)

### 新增
- **帮助中心 / 关于页补全**：帮助页新增「前置安装条件」链接卡片（Tampermonkey / ScriptCat）及打开方法、立即生效说明、功能失效排查指引；关于页新增 GitHub 仓库与 ScriptCat 主页卡片式按钮（版本/许可/致谢字块）。
- **对话文件夹树行图标区分展/折**：折叠态用空心文件夹、展开态切换实心打开文件夹 SVG（fill=currentColor 随主题取色）。
- **文件夹树当前会话行选中指示条**：树内当前会话行（`.on`）标题左侧显示 3px 主题色竖条（`dsConvBar`，仅选中态渲染）。

### 变更（设置面板风格对齐 Ant Design）
- **主色**：Indigo `#6366f1` → antd 蓝 `#1677ff`（hover `#0958d9`）；聚焦光环、开关、下拉选中、链接卡片 hover、侧栏激活竖条/底色等 10 处联动换色（浅/深双主题）。
- **边框中性化**：分隔线 `#eef1f5 → #f0f0f0`（colorBorderSecondary）、控件边框 `#d7dce3 → #d9d9d9`（colorBorder），去除蓝灰调。
- **圆角阶梯归一**：面板 18 → 8（borderRadiusLG）、卡片/下拉 8、输入/下拉触发器/按钮/导航项/关闭钮 6（borderRadius）。
- **投影减重**：面板 `0 24px 64px .35` → `0 12px 40px .18`；下拉浮层同步减轻。
- **边距对齐 8px 栅格**：内容区统一 24px、顶栏 46 → 48 高、底部条左右 18 → 24px。
- **字号归一 antd 字阶**：设置标题 14、输入/下拉/选项 14（原 13.5）、分区标题 20（原 21）。
- **右区子页面排版卡片化**：每页设置行/说明块/链接卡片统一包进白底卡片（`.ds-p-card`，圆角 8、细边），分隔线收进卡内，与 `#f7f8fa` 页面底色形成层次；副标题去掉通栏分隔线；窄屏下卡片横向内边距收窄至 12px。

## v4.8.0 (2026-09-06)

### 新增
- **设置面板重构为「左导航 + 右分区页」布局**：面板由单列深色卡片堆叠改为左右两栏——左侧深蓝渐变导航（logo + 分区项 + 竖条激活态 + 分隔线），右侧顶栏（✕）之下按分区分别展示，切换带简入动画；分区默认展开「代码块折叠」。
- **对设置分区分组浏览**：六个分区「代码块折叠 / 表格优化导出 / AI 思考折叠 / 宽屏模式 / 聊天发送 / 对话文件夹」各自独立成页，外加说明性的「帮助中心 / 关于」入口（导航第二区），不再整列一屏塞满。
- **设置项改为「行式」排布**：每项标签与中文描述在左、开关/下拉/输入在右，细分隔线，参考 waitadd/设置面板结构.html 的控件交互；开关改为无痕 slider 风格。
- **深浅双主题自适应**：右区颜色改用 CSS 变量 `--dsp-*`，默认浅色，`body.dark .ds-panel` 整组覆盖为深色；左侧导航保持深蓝渐变两种主题一致。
- **图标源对齐 iconify**：面板/导航/分区标题所用作图标 emoji 全面替换为 `api.iconify.design/mdi:*` SVG（代码折叠/表格/思考/宽屏/发送/文件/帮助/关于/齿轮）；右侧标题图标按当前主题取色，左侧导航固定亮色。
- **窄屏响应式**：`@media` 低于 640px 时面板改为纵向、左导航转横排分组、设置项纵向堆叠、输入/下拉占满宽度。
- **新增帮助中心 / 关于静态页**：`createInfoIntro` 说明字块展示打开方式、即时生效/自动保存、失效排查、版本/许可/致谢。

### 修复
- **表格导出按钮 SVG 与 emoji 同屏叠加**：改为按钮只渲染 iconify SVG，加载失败时以 `onerror` 整块替换为对应 emoji——任一时点仅显示一个图标。
- **导出按钮图片叠层回归**：移除 `.export-btn-ic` 的绝对居中叠层，img 随按钮 flex 布局居中。

### 优化
- 「脚本设置」菜单命令去除 ⚙️ 前缀改纯文字；面板内容滚动条细窄半透明风格转由右侧内容滚动区承接。

## v4.7.1 (2026-09-05)

### 修复
- **文件夹面板偶发不随会话列表滚动**：根因是 `ensurePanel` 在会话链接尚未渲染的加载/导航竞态下，`findScrollContainer` 返回 null 后把面板兜底插到「新对话」按钮之后（滚动容器外），再加 `getElementById` 早退守卫导致错误位置被永久锁定。现：`findScrollContainer` 增加兜底——链接缺失时从「新对话」按钮向上定位侧栏根、再于其后代中寻找唯一滚动容器；删除「插到新对话按钮后」的容器外降级；`ensurePanel` 对面板做自修复重挂（若被误插到别处自动迁移回滚动容器，升级前残留一并校正）。
- **文件夹面板与原生「多选」按钮视觉重叠**：面板正确进入会话列表顶部后，原生绝对定位的「多选」按钮（固定悬浮于列表视口右上）压在面板顶部。`#dsFolderPanel` 顶部内边距 6px → 22px 预留悬浮按钮行，面板内容整体下移至按钮之下。
- **文件夹树稳态下反复重建（无限刷新循环）**：`ensurePanel` 快路径守卫只检查面板直接父级是否为滚动容器，但面板实际经「置顶」sticky 行挂在内容包装层（overflow:visible）下、真正的列表 scroller 在其上方若干层——守卫永不成立 → 每轮 schedule 都重挂并 `renderFolders` 重建 `.dsList`（真实 mutation → observer → schedule 自激）。改为沿祖先链判定「是否已在滚动容器内」，稳态零额外渲染。
- **面板折叠态刷新后首挂载不生效**：`setCollapsedUI` 原在面板入 DOM 前调用（`getElementById` 找不到 → no-op），`data.collapsed=true` 需点一次标题才生效。改为面板挂载后再调用。

### 优化
- `ensurePanel` 稳态路径零全量扫描：面板已随列表滚动时直接早退（仅一次 `getComputedStyle` 祖先链判定），不再每轮调用 `findScrollContainer`。

## v4.7.0 (2026-09-04)

### 新增
- **对话文件夹管理**（并入自独立脚本 waitadd/deepseek-folder-manager，对齐 v0.9.2 树形交互）：左侧对话历史栏加入「文件夹」面板。文件夹以树形就地展开/收起（空心文件夹图标、展开态持久化），其下内嵌该文件夹的会话行（当前会话高亮、可点击跳转官方会话、每行「移出」；会话行仅显示标题（超长自动省略号、悬停显示完整标题）、不展示内部会话 id）；每行可通过 ⋯ 菜单 →「移动到文件夹」归入/移出（悬停即开的级联浮层 + 标题彩色标签）；**已收进子文件夹的会话从原生历史列表自动隐藏**，避免点树内会话时官方滚动回原位（路由切换才重绘树，observer 不会自激）。
- **表格导出新增 Markdown 一键复制**（📝，社区 PR #1 落地）：单元格内 `code` / **加粗** / *斜体* 保留为行内 Markdown，`|` 管道符转义；表头+分隔行 `---`，无表头自动补。
- **抽取 `getCleanTableClone` 复用**：PNG/CSV/MD 三种导出共用同一份“深拷贝 + 清洗注入样式/导出按钮/脚本标记”逻辑，避免各自复制、并防止读取页面被样式的表格。
- **发送快捷键可开关**（⌨️ 聊天发送设置卡）：开启后 Ctrl/Cmd+Enter 发送、纯 Enter 换行（默认关，恢复默认会一并重置）；关闭切回官方 Enter 发送 / Shift+Enter 换行。
- **控制面板新增开关卡片**「🗂 对话文件夹管理」：默认**关闭**（opt-in）。关闭时不注入侧栏面板/菜单项/样式，且会整体清理已应用的 DOM（还原被归档隐藏的原生会话行、移除标签/菜单项/样式与 CSS 变量）
- **并入细节**
  - 数据存独立新键 `deepseek_folder_manager_data_v2`（含文件夹、归属 `links`、树展开态 `expanded`），与旧独立脚本（`ds_folder_manager_v1` 键）互不干扰、不动老数据
  - 保留原脚本主题逻辑，随官网浅/深色自适应（浅/深 CSS 变量均由动态注入、移除）
  - DOM 监听并入主脚本统一 MutationObserver，不再各占一个 body observer；开关/路由切换（含导航守卫防自循环）由 observeDOM 打标记 + init 首启驱动
  - 子闭包 `folderUnit` 封装，避免污染主脚本命名空间
- **渲染层升级记录**：主脚本首次并入按 v0.8.1（抽屉 + 全局筛选）实现 → 本版改对齐 waitadd v0.9.x（退出抽屉/全局筛选模型，改树形就地展开 + 归档隐藏 + 空心文件夹图标 v0.9.2）。
- **文件夹面板标题可整体折叠/展开**：点面板头的「文件夹」标题即可收起/展开整个文件夹列表（箭头随状态转向，展开态持久化于 `data.collapsed`）；收起时列表 `display:none` 不再占位滚动；点「＋ 新建」自动展开以立即看到新夹。可选模块默认关闭、keyboard 可达（可聚焦、Enter/Space 触发）。

### 修复
- **去掉文件夹区内部 300px 独立滚动**：`.dsList` 不再自设 `max-height:300px; overflow:auto`，整个文件夹区随原生会话历史一起滚动，区内不再单独出滚动条
- **不再对官方 `.ds-floating-container` 设 `display:none` 来收菜单**：官网浮层容器跨会话复用（React portal 一次创建、此后打开只切内容不复建），若在「移到文件夹/新建」后对其永久藏死，会导致该会话之后再点 ⋯ 时内容只渲染进这个隐藏容器 → “完全点不开/无反应”。真机（MCP Chrome）实测确认页面残留有 `display:none` 且 in-DOM 的空浮层容器，与旧代码产物吻合。修复为只移除我们自己的浮层，把菜单收敛交给官网自带关闭语义。

### 贡献者
- **@Chuc-Jie**（作者/维护者）：主导 v4.7.0 功能收编与发布
- **@wha4up**（社区贡献者）：提出 [PR #1](https://github.com/Chuc-Jie/deepseektool/pull/1)，为 v4.7.0 带来表格 Markdown 一键复制、导出克隆清洗复用、发送快捷键等方向
- 感谢所有反馈与支持的用户

## v4.6.1 (2026-09-03)

### 新增
- **对话文件夹管理**（并入自独立脚本 waitadd/deepseek-folder-manager，对齐 v0.9.2 树形交互）：左侧对话历史栏加入「文件夹」面板。文件夹以树形就地展开/收起（空心文件夹图标、展开态持久化），其下内嵌该文件夹的会话行（当前会话高亮、可点击跳转官方会话、每行「移出」；会话行仅显示标题（超长自动省略号、悬停显示完整标题）、不展示内部会话 id）；每行可通过 ⋯ 菜单 →「移动到文件夹」归入/移出（悬停即开的级联浮层 + 标题彩色标签）；**已收进子文件夹的会话从原生历史列表自动隐藏**，避免点树内会话时官方滚动回原位（路由切换才重绘树，observer 不会自激）。
- **控制面板新增开关卡片**「🗂 对话文件夹管理」：默认**关闭**（opt-in）。关闭时不注入侧栏面板/菜单项/样式，且会整体清理已应用的 DOM（还原被归档隐藏的原生会话行、移除标签/菜单项/样式与 CSS 变量）
- **并入细节**
  - 数据存独立新键 `deepseek_folder_manager_data_v2`（含文件夹、归属 `links`、树展开态 `expanded`），与旧独立脚本（`ds_folder_manager_v1` 键）互不干扰、不动老数据
  - 保留原脚本主题逻辑，随官网浅/深色自适应（浅/深 CSS 变量均由动态注入、移除）
  - DOM 监听并入主脚本统一 MutationObserver，不再各占一个 body observer；开关/路由切换（含导航守卫防自循环）由 observeDOM 打标记 + init 首启驱动
  - 子闭包 `folderUnit` 封装，避免污染主脚本命名空间
- **渲染层升级记录**：主脚本首次并入按 v0.8.1（抽屉 + 全局筛选）实现 → 本版改对齐 waitadd v0.9.x（退出抽屉/全局筛选模型，改树形就地展开 + 归档隐藏 + 空心文件夹图标 v0.9.2）。

### 修复
- **去掉文件夹区内部 300px 独立滚动**：`.dsList` 不再自设 `max-height:300px; overflow:auto`，整个文件夹区随原生会话历史一起滚动，区内不再单独出滚动条
- **不再对官方 `.ds-floating-container` 设 `display:none` 来收菜单**：官网浮层容器跨会话复用（React portal 一次创建、此后打开只切内容不复建），若在「移到文件夹/新建」后对其永久藏死，会导致该会话之后再点 ⋯ 时内容只渲染进这个隐藏容器 → “完全点不开/无反应”。真机（MCP Chrome）实测确认页面残留有 `display:none` 且 in-DOM 的空浮层容器，与旧代码产物吻合。修复为只移除我们自己的浮层，把菜单收敛交给官网自带关闭语义。

## v4.5.2 (2026-06-29)

### 修复
- **表格 table-layout:fixed CSS 预应用**：将 `table-layout: fixed` 从 JS 提升到 CSS 中预应用，表格从创建起即用 fixed 布局渲染，高度不再因 JS 二次应用而变化。根除长表格（100行+）刷新后视口漂移到几十行位置的 bug（auto→fixed 高度变化可达 +44%）
- **Release workflow**：awk 退出条件 `/^#{2,3} v/` 同时匹配 `##` 和 `###` 版本标题；CHANGELOG v4.5.0 恢复为 `##` 顶级标题

## v4.5.1 (2026-06-29)

### 修复
- **表格预隐藏消除样式闪烁**：CSS `opacity: 0` 使表格从创建起不可见，`applyTableStyles` 完成所有测量和内联样式后设置 `opacity: 1`，0.12s 淡入，用户不再看到原始样式→优化样式的过渡闪变
  - PNG 导出保护：clone 显式 `opacity: 1` 防止被带入 iframe
- **Release workflow**：修复 awk 匹配 `$` 锚点（CHANGELOG 行含日期后缀导致匹配失败），改用 `index` 前缀匹配；改用 `body_path` 文件方案替代多行 `${{ }}` YAML 变量注入

## v4.5.0 (2026-06-29)

### 新增
- **宽屏模式**：消息区域从 752px → 1000px，左右留白自动均分；面板 Toggle 开关控制，默认关闭；表格列宽通过 `vc.clientWidth` 联动扩展
  - 参考 `docs/豆包Deepseek 消息宽屏.js`
  - 改用 `--message-list-max-width: 1000px !important` 覆盖 CSS 变量，避免暴力改 padding 导致的不对称
- **思考内容 CSS 预隐藏 + capture 点击释放**：零布局偏移
  - 注入 `display: none !important` 样式，使 `.ds-think-content` 从创建起即不可见
  - capture 阶段监听标题栏点击，在 DeepSeek 事件处理前移除 CSS，toggle 正常
  - 彻底解决旧 simulate-click 方案的"展开→可见→折叠"跳闪

### 修复
- **Release workflow**：新增 awk 步骤解析 `CHANGELOG.md` 当前版本条目，自动填入 Release body，替代静态文案

### UI
- **面板新增**：宽屏模式 Toggle 卡片

## v4.4.1 (2026-06-28)

### UI
- **控制面板 HIG 优化**：卡片分区（ds-panel-card）替代 border-top 分割线；复选框升级为 Toggle 开关（44×24px，thumb 滑动动效）；控件工厂化（createCard/createToggle/createSelect/createNumberSetting）；数字输入增加单位后缀；底部新增「恢复默认设置」一键重置
- **面板宽度** 420px → 480px，背景 rgba(0,0,0,0.45) + backdrop-filter:blur(6px) 微调

### 修复
- **下拉框深色适配**：弃用原生 `<select>`（选项区 OS 渲染无法控制），改用自定义 div 组件（.ds-custom-select），选项 hover/active 态完全可控
- 添加 `-webkit-appearance:none` 修复 Chromium 内核下原生箭头残留
- 修复 toggle CSS 选择器 bug（`~ .ds-toggle-thumb` → `+ .ds-toggle-track .ds-toggle-thumb`）

## v4.4.0 (2026-06-24)

### 新增
- **表格主题适配双方案**：解决表格硬编码浅色配色在深色模式下视觉错乱的问题
  - **方案 A「自动适应」**：使用 `rgba()` 半透明叠加色（如 `border: rgba(128,128,128,0.2)`），不区分浅色/深色，自动通透
  - **方案 B「双模式」**：通过 `body.dark` / `body:not(.dark)` 选择器为两种主题定义独立配色
  - 控制面板「表格优化导出」区新增「表格主题适配」下拉框，即时切换无需刷新
- **表格列宽策略三模式**：
  - **均分**：`table-layout: fixed` + 每列等宽（默认）
  - **自适应**：`table-layout: fixed` + 采样内容文本长度按比例分配列宽百分比
  - **均分+保护**：均分 + `min-width: 80px`，当 `80px × 列数 > 容器宽度` 时自动回落自适应并 Toast 提示
  - 控制面板「表格优化导出」区新增「表格列宽策略」下拉框
- **PNG 导出跟随主题**：`collectTableStyles` 运行时检测当前模式和 `body.dark` 状态，动态生成匹配的回退 CSS；iframe body 背景取计算值

### 修复
- **自适应模式溢出**：`table-layout: auto` 在内容较宽时无视 `max-width` 穿破容器（实测 6 列 100 行从 752px 溢出到 801px），改为 `fixed` + 内容比例分配方案彻底解决
- 移除所有无效的 `var(--ds-xxx)` CSS 变量引用（这些变量在 DeepSeek 中实际不存在，永远回落为浅色值）
- 三种列宽模式的 `width`/`maxWidth` 统一由 `applyTableStyles` 顶部提取 `maxW` 变量派发

### 架构
- `GM_addStyle` 表格 CSS 拆为三层：公共布局（无颜色）+ Plan A 规则 + Plan B 规则
- 通过 `html.ds-table-auto` / `html.ds-table-dual` 类切换，CSS 选择器自动生效
- 提取 `calcColumnWeights()` 公共函数，避免自适应和回落分支的代码重复

## v4.2.1 (2026-06-20)

### 修复（审计）
- **状态机加 done 标记**：已稳定表格不再每轮重复调用 applyTableStyles，消除冗余 reflow
- **超时 firstSeen 重置**：防止首次超时后进入永久超时状态导致每 200ms 重复应用
- **Observer 补检**：添加 `tbody`/`thead`/`tfoot` 匹配，覆盖框架批量插入场景
- **清理 wordWrap 死代码**：导出克隆清洗移除不再被设置的 wordWrap 属性

## v4.2.0 (2026-06-19)

### 优化
- **稳定性计数替代时间防抖**：表格处理从"等 500ms"改为"连续 2 次指纹不变即应用"，更精准响应；新增 5s 超时兜底
- **`overflow-wrap: anywhere`** 替代非标准 `word-break: break-word`，现代浏览器兼容更好

## v4.1.4 (2026-06-19)

### 修复
- **页面刷新后表格样式延迟**：首次见到表格立即应用样式，指纹稳定检查仅对已见过的表格生效，消除刷新后 ~500ms 的白等

## v4.1.3 (2026-06-19)

### 修复
- **PNG 导出修复**：解决表格 PNG 导出损坏问题。最终方案为隔离 iframe (srcdoc) 渲染，深克隆表格并清洗 fixed 内联样式，html2canvas 截图后通过 toBlob → blob URL 导出，导出图片中表格为 auto 布局。期间解决：data URL 截断、allowTaint canvas 污染、toBlob() 返回 null、多列 fixed 布局冲突等连锁问题。
- 新增 collectTableStyles() 自动收集页面 CSS 变量注入 iframe

## v4.1.2 (2026-06-19)

### 修复
- **表格流式输出**：新增表格指纹稳定检测，样式只在表格停止变化后才应用，解决流式输出中表格显示错乱
- MutationObserver 表格检测补全 `<tr>`/`<td>`/`<th>` 增量节点，防止仅行/列变化时漏检
- 表格处理防抖从 200ms 提升至 500ms，给流式渲染更充裕的稳定窗口

## v4.1.1 (2026-05-29)

### 修复
- 适配 DeepSeek 新版代码块按钮（`.ds-text-button` → `.code-info-button-text`），恢复折叠按钮在原生按钮组内的正常位置

## v4.1.0 (2026-05-29)

### 选择器加固
- 代码块折叠按钮容器不再依赖哈希类名，改用 `.ds-text-button` 父元素定位
- AI 思考区域改用稳定的 `.ds-think-content` 类名定位，移除 `querySelectorAll('*')` 全量扫描
- 文本扫描自动跳过 `<pre>` 和 `.md-code-block`，避免对话中的脚本源码误触发

### 功能修复
- **思考折叠回退**：关闭"模拟点击"时，改用 CSS 直接隐藏思考内容（之前该项关闭后折叠完全失效）
- **CSV 导出**：修复单元格内换行（`<br>`）被压缩消失的问题
- **表格样式**：仅修改最近的 `.ds-scroll-area` 容器，不再遍历祖先生改 `overflow`

### 性能优化
- 三个独立 MutationObserver 合并为一个，减少重复 DOM 遍历
- 表格处理增加 200ms 防抖，避免 AI 流式输出时频繁触发

### 其他
- 移除 CSS 中 `.efa13877 .ds-fold-btn` 硬编码选择器
- `deduplicateButtons` 改用按钮结构去重，不再依赖哈希类名
