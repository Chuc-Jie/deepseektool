# 🧰 DeepSeek 功能增强工具箱

> 为 DeepSeek Chat 打造的增强工具箱：长代码自动折叠、表格修复与一键导出、AI「已思考」过程自动收起、对话文件夹分组。安装即用，全部设置收进同一个 ⚙️ 控制面板——**调整立即生效，刷新页面不丢失**。

## 📖 简介

一款专为 DeepSeek 对话页（chat.deepseek.com）设计的用户脚本。它解决日常使用中最常见的几个痛点：长代码块刷屏、表格过窄难看又难导出、AI 思考过程太长、历史对话杂乱无分类。所有功能集中在一个「⚙️ 脚本设置」面板里可视化配置，打开即用，无需修改任何代码。

## ✨ 功能一览

| 功能 | 位置 / 触发方式 | 默认状态 |
| --- | --- | --- |
| 代码块折叠 | 每个代码块右上角「展开 / 折叠」按钮，超长代码自动收起 | 超过 20 行自动折叠 |
| 表格样式修复 | 自动应用于消息中的所有表格 | 始终生效 |
| 表格导出 | 鼠标悬停表格 → 右下角 📸 PNG / 📄 CSV / 📝 Markdown | 开启（悬停显示） |
| AI 思考过程自动收起 | 新回复的「已思考」过程自动折叠，点击标题即可展开 | 开启 |
| 宽屏模式 | 消息区加宽、减少左右留白 | 关闭 |
| Ctrl+Enter 发送 | 输入框内 Enter 换行、Ctrl+Enter 发送，防误触 | 关闭 |
| 对话文件夹分组 | 左侧「对话历史」的文件夹面板 + 会话 ⋯ 菜单归类 | 关闭（按需开启） |

## 🚀 三步上手

1. 安装脚本后刷新 DeepSeek 对话页；
2. 点击浏览器工具栏的 **Tampermonkey / ScriptCat 图标 → 本脚本 →「⚙️ 脚本设置」**，打开统一控制面板；
3. 按需勾选 / 调整各项开关——**立即生效并自动保存**，下次打开页面保持。

## 🧩 功能说明

### 📁 代码块折叠
- 行数超过「自动折叠阈值」的代码块自动收起，右上角提供「展开 / 折叠」按钮（含箭头图标）手动切换；
- 阈值设为 `0` 即完全关闭自动折叠；
- 「折叠预览行数」设为大于 0 时，收起后仍保留前 N 行可见；
- **手动展开过的代码块不会因后续对话加载再次被折叠**（状态保持）。

### 📊 表格优化与导出
- **样式修复始终生效**：表格宽度自适应、列宽按策略分配、圆角边框、行悬停高亮；
- **导出按钮（可开关）**：鼠标悬停表格，右下角浮现三个按钮——
  - 📸 **PNG**：截图保存为图片，自动跟随当前浅 / 深色主题；
  - 📄 **CSV**：原生 UTF-8 BOM，Excel 直接打开不乱码；
  - 📝 **Markdown**：转为表格语法一键复制（保留单元格内 `code` / 加粗 / 斜体并转义 `|`）；
- 开启「持续显示导出按钮」后按钮常驻、无需悬停；
- 可选**主题适配**（自动适应，半透明叠加通吃浅深色 / 双模式，各自优化配色）与**列宽策略**（均分 / 按内容自适应 / 均分 + 最小宽度保护），均在面板切换。

### 🧠 AI 思考过程自动折叠
- AI 回复中的「已思考」过程自动收起，页面不再冗长；想看推理时点击「已思考」标题即可展开；
- 默认用**模拟点击**折叠（与官方箭头交互一致）；若个别页面异常，可在面板切换为 **CSS 直接折叠**。

### 🗂 对话文件夹分组（可选，默认关闭）
- 在面板开启「启用文件夹分组」后，左侧「对话历史」顶部出现 **[文件夹] ＋ [＋新建]** 面板，随历史列表一起滚动；
- 归类：鼠标悬停任意会话的三点 **⋯ →「移动到文件夹」**，悬停即展开文件夹列表，点击即归入；也可直接选「＋新建文件夹…」或「移出文件夹」；
- 收进文件夹的会话**自动从官方历史列表隐藏**，只在文件夹内显示——点文件夹行前的空心图标就地展开树，当前打开的会话高亮、点击可跳转，每行右侧「移出」可还原；
- 会话标题旁显示所属文件夹的**彩色标签**，随浅色 / 深色主题自适应；
- 面板标题可整体收起 / 展开（状态持久化）；**关闭开关**会还原全部原生会话并移除标签与面板，但已保存的分组数据保留，再次开启不丢失。

### 🖥️ 宽屏模式
- 开启后消息区扩展至全宽，左右留白自动均分，长文本阅读更舒适；关闭即恢复官方宽度。

### ⌨️ Ctrl+Enter 发送
- 开启后：**Enter 换行、Ctrl+Enter（Mac：Cmd+Enter）发送**，Shift+Enter 始终为换行——防止手滑误发；
- 关闭即恢复官方行为：Enter 发送、Shift+Enter 换行。

## ⚙️ 控制面板一览

面板按分区卡片组织，底部另有「恢复默认设置」一键还原全部项：

| 分区 | 设置项 | 默认 | 说明 |
| --- | --- | --- | --- |
| 📦 代码块折叠 | 自动折叠阈值 | `20` 行 | 代码行数超过该值自动折叠；`0` = 禁用 |
| 📦 代码块折叠 | 折叠预览行数 | `0` | 折叠后保留的行数；`0` = 完全隐藏 |
| 📊 表格优化导出 | 表格导出按钮 | 开 | 悬停表格显示 📸 📄 📝 按钮 |
| 📊 表格优化导出 | 持续显示导出按钮 | 关 | 无需悬停，导出按钮始终可见（需上一项开启） |
| 📊 表格优化导出 | 表格主题适配 | 自动适应 | 半透明叠加通用 / 浅色深色双模式 |
| 📊 表格优化导出 | 表格列宽策略 | 均分列宽 | 均分 / 按内容自适应 / 均分 + 80px 下限保护 |
| 🧠 AI 思考过程折叠 | 自动折叠思考区域 | 开 | AI 开始思考后自动收起「已思考」过程 |
| 🧠 AI 思考过程折叠 | 模拟点击折叠 | 开 | 关闭后改用 CSS 直接折叠 |
| 🖥️ 宽屏模式 | 启用宽屏布局 | 关 | 消息区扩展至全宽 |
| ⌨️ 聊天发送设置 | Ctrl+Enter 发送 | 关 | Enter 换行、Ctrl+Enter 发送 |
| 🗂 对话文件夹管理 | 启用文件夹分组 | 关 | 左侧文件夹面板 + ⋯ 菜单归类 |

## 🛠️ 兼容性

- 适用站点：`chat.deepseek.com`、`www.deepseek.com`、`deepseek.com`
- 脚本管理器：Tampermonkey / ScriptCat 通用
- 外部依赖：`html2canvas@1.4.1`（仅 PNG 导出需要）
- 基于 DeepSeek 当前页面结构编写；官方大版本更新可能导致部分功能失效，届时欢迎反馈以便适配

## 📄 许可证与反馈

- **MIT License**，完全开源、自由使用与修改；
- 源码与更新日志：[GitHub · Chuc-Jie/deepseektool](https://github.com/Chuc-Jie/deepseektool)
- 问题与建议：GitHub Issues，或本页评论区留言，感谢使用 🙏
